# Copy to .secrets and fill in. Do not commit .secrets.
# Used by render-bootstrap-multi-app.sh for zero-prompt runs.

export RENDER_API_KEY=your-render-api-key
# Optional: set if connection-info is not ready yet (script will use this or prompt)
# export DATABASE_URL=postgres://...
